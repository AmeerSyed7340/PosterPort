version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd client
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: client/dist
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*

  - backend:
      phases:
        preBuild:
          commands:
            - cd server
            - npm install
      artifacts:
        baseDirectory: server
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
